---

- name: Configure and git clone
  hosts:
    - downloader
    - plex
    - dev
  roles:
    - docker-git
  become: yes

- name: Configure and run docker containers
  hosts:
    - downloader
    - plex
    - dev
  roles:
    - docker-restart
  become: yes
