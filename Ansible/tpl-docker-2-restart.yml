---

- name: Configure and git clone
  hosts:
    - downloader
    - plex
    - dev
    - docker_camp
  roles:
    - docker-git
  become: yes

- name: Configure and run docker containers
  hosts:
    - downloader
    - plex
    - dev
    - docker_camp
  roles:
    - docker-restart
  become: yes
